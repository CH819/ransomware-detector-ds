events {}

http {
  include /etc/nginx/mime.types;
  sendfile on;

  server {
    listen 5173;
    listen [::]:5173;
    root /usr/share/nginx/html;

    location / {
      try_files $uri $uri.html $uri/ =404;
    }

    location = /404.html {
      internal;
    }
  }
}
